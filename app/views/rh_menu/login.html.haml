%h1.text-center Login
%div.text-center
	= form_tag(:action => 'validate_login') do
		%p
		Username: #{text_field_tag :username}
		%p
		Password: #{password_field_tag :login_password}
		%br
		%br
			= submit_tag "Log In", :class => "btn btn-danger"